name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: 24ds2000112@ds.study.iitm.ac.in
        run: echo "Hello, world!"
